export default function SectionTitle({ title }: { title: string }) {
    return (
        <div className="ml-5 text-center text-lg font-light uppercase text-gray-900">
            {title}
        </div>
    )
}
