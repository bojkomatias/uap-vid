export default function SectionTitle({ title }: { title: string }) {
    return (
        <div className="ml-5 text-center uppercase font-light text-lg text-gray-900">
            {title}
        </div>
    )
}
