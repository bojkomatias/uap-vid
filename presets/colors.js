const colors = require('tailwindcss/colors')

module.exports = {
    presets: [],
    theme: {
        colors: {
            current: 'currentColor',
            base: colors.neutral,
            primary: colors.pink,
            secondary: colors.rose,
        },
    },
}
