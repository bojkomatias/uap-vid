const colors = require('tailwindcss/colors')

module.exports = {
    presets: [],
    theme: {
        colors: {
            current: 'currentColor',
            base: colors.neutral,
            primary: '#003C71',
            secondary: colors.gray,
            white: colors.white,
            black: colors.black,
        },
    },
}
